# راهنمای فاز ۸: پنل وب مدیریتی / Phase 8: Web Admin Panel

## 🎯 هدف
ساخت پنل وب برای مدیریت سیستم تیکتینگ (React + TypeScript + Vite) با صفحات پایه: ورود، داشبورد، لیست تیکت‌ها، جزئیات تیکت.

## ✅ انتخاب فناوری
- React 18 + TypeScript
- Vite برای توسعه سریع
- Fetch/axios برای ارتباط با API (در حال حاضر از fetch ساده استفاده می‌کنیم)
- ساده‌سازی اولیه UI (بدون CSS framework – قابل افزودن Tailwind در ادامه)

## 🚀 راه‌اندازی
1) نصب وابستگی‌ها:
```bash
cd web_admin
npm install
npm run dev
```
سپس در مرورگر به آدرس نشان‌داده‌شده (معمولاً http://localhost:5173) بروید.

2) تنظیم آدرس API:
- فایل `web_admin/src/services/api.ts` مقدار `API_BASE_URL` را از `.env` یا مقدار پیش‌فرض `http://localhost:8000` می‌خواند. در صورت نیاز ویرایش کنید.

## 📁 ساختار پوشه
```
web_admin/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── src/
    ├── App.tsx
    ├── main.tsx
    ├── pages/
    │   ├── Dashboard.tsx
    │   ├── Login.tsx
    │   └── Tickets.tsx
    └── services/
        └── api.ts
```

## 🧩 صفحات
- Login: دریافت توکن و ذخیره در localStorage
- Dashboard: خوش‌آمدگویی و لینک‌ها
- Tickets: لیست تیکت‌ها با صفحه‌بندی ساده و لینک به جزئیات
- Ticket Detail (گام بعدی): مشاهده و تغییر وضعیت (ادمین)

## 🔐 احراز هویت
- پس از ورود، توکن در localStorage ذخیره و در Header درخواست‌ها ارسال می‌شود.
- در نبود توکن، کاربر به صفحه Login هدایت می‌شود.

## 🧪 تست
1) API را اجرا کنید:
```bash
uvicorn app.main:app --reload
```
2) وب‌اپ را اجرا کنید:
```bash
cd web_admin
npm run dev
```
3) با کاربر `admin/admin123` وارد شوید و لیست تیکت‌ها را ببینید.

## 📌 گام‌های بعدی
- استایل‌دهی (Tailwind)
- فیلترهای پیشرفته و جستجو
- صفحه جزئیات تیکت و تغییر وضعیت
- مدیریت کاربران/شعب (در فازهای بعدی)

---

**تاریخ:** 2025-11-11


# 📋 راهنمای کامل فیلدهای سفارشی (Custom Fields)

## فهرست مطالب
1. [معرفی](#معرفی)
2. [ویژگی‌ها](#ویژگی‌ها)
3. [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
4. [استفاده از Frontend](#استفاده-از-frontend)
5. [انواع فیلدها](#انواع-فیلدها)
6. [تنظیمات پیشرفته](#تنظیمات-پیشرفته)
7. [API Reference](#api-reference)
8. [بهترین روش‌ها](#بهترین-روش‌ها)

---

## معرفی

سیستم فیلدهای سفارشی (Custom Fields) یک سیستم پیشرفته و حرفه‌ای برای افزودن فیلدهای دینامیک به تیکت‌ها است. این سیستم به شما امکان می‌دهد بدون نیاز به تغییر کد، فیلدهای جدید به تیکت‌ها اضافه کنید.

### کاربردها:
- ✅ افزودن اطلاعات اضافی به تیکت‌ها (مثلاً شماره پروژه، نام مشتری، و غیره)
- ✅ فیلتر و جستجوی پیشرفته‌تر
- ✅ گزارش‌گیری دقیق‌تر
- ✅ سفارشی‌سازی سیستم بر اساس نیاز سازمان

---

## ویژگی‌ها

### ✨ ویژگی‌های کلیدی:
- ✅ **10 نوع فیلد مختلف**: Text, Textarea, Number, Date, DateTime, Boolean, Select, MultiSelect, URL, Email, Phone
- ✅ **فیلتر پیشرفته**: بر اساس Category, Department, Branch
- ✅ **تنظیمات پیشرفته**: Required, Visible, Editable, Default Value, Help Text, Placeholder
- ✅ **اعتبارسنجی کامل**: بر اساس نوع فیلد و Config
- ✅ **UI حرفه‌ای**: کامپوننت‌های React زیبا و Responsive
- ✅ **مدیریت کامل**: ایجاد، ویرایش، حذف از پنل مدیریت
- ✅ **یکپارچه‌سازی کامل**: با فرم ایجاد تیکت و جزئیات تیکت

---

## نصب و راه‌اندازی

### 1. اجرای Migration

برای ایجاد جداول فیلدهای سفارشی:

```bash
python scripts/migrate_v16_create_custom_fields.py
```

**خروجی موفق:**
```
INFO:__main__:Migration v16 completed: Created custom_fields and ticket_custom_field_values tables
```

### 2. بررسی Backend

Backend به صورت کامل پیاده‌سازی شده است:
- ✅ مدل‌های دیتابیس
- ✅ Service Layer
- ✅ API Endpoints
- ✅ تست‌های کامل

### 3. Frontend

Frontend به صورت کامل پیاده‌سازی شده است:
- ✅ صفحه مدیریت Custom Fields
- ✅ کامپوننت رندر فیلدها
- ✅ یکپارچه‌سازی با فرم‌ها

---

## استفاده از Frontend

### صفحه مدیریت Custom Fields

#### دسترسی:
1. وارد پنل مدیریت شوید
2. به منوی "📋 فیلدهای سفارشی" بروید
3. یا مستقیماً به `/custom-fields` بروید

#### ایجاد فیلد جدید:

1. **کلیک روی "➕ افزودن فیلد جدید"**

2. **پر کردن فرم:**
   - **نام فیلد (Internal)**: نام داخلی فیلد (فقط حروف، اعداد، خط تیره و زیرخط)
     - مثال: `project_name`, `customer_phone`
   - **برچسب (فارسی)**: نام نمایشی فیلد
     - مثال: "نام پروژه", "شماره تماس مشتری"
   - **برچسب (انگلیسی)**: نام نمایشی انگلیسی (اختیاری)
   - **نوع فیلد**: انتخاب نوع فیلد از لیست
   - **دسته‌بندی تیکت**: انتخاب دسته‌بندی (یا "همه دسته‌بندی‌ها")
   - **دپارتمان**: انتخاب دپارتمان (یا "همه دپارتمان‌ها")
   - **شعبه**: انتخاب شعبه (یا "همه شعب")
   - **ترتیب نمایش**: عدد کمتر = نمایش زودتر

3. **تنظیمات پیشرفته:**
   - ☑️ **الزامی**: آیا فیلد اجباری است؟
   - ☑️ **قابل مشاهده برای کاربر**: آیا کاربر می‌تواند فیلد را ببیند؟
   - ☑️ **قابل ویرایش توسط کاربر**: آیا کاربر می‌تواند فیلد را ویرایش کند؟
   - ☑️ **فعال**: آیا فیلد فعال است؟

4. **تنظیمات Config (بر اساس نوع فیلد):**

   **برای SELECT/MULTISELECT:**
   - کلیک روی "➕ افزودن گزینه"
   - وارد کردن "مقدار" (value) و "برچسب" (label)
   - مثال:
     - مقدار: `urgent`, برچسب: "فوری"
     - مقدار: `normal`, برچسب: "عادی"

   **برای NUMBER:**
   - حداقل (min): حداقل مقدار مجاز
   - حداکثر (max): حداکثر مقدار مجاز
   - Step: گام تغییر مقدار

5. **ذخیره فیلد**

#### ویرایش فیلد:

1. کلیک روی "✏️ ویرایش" در ردیف فیلد
2. تغییر تنظیمات مورد نظر
3. کلیک روی "💾 ذخیره"

**نکته:** نام فیلد (Internal) پس از ایجاد قابل تغییر نیست.

#### حذف فیلد:

1. کلیک روی "🗑️ حذف" در ردیف فیلد
2. تایید حذف

**⚠️ هشدار:** حذف فیلد، تمام مقادیر آن در تیکت‌ها را نیز حذف می‌کند.

#### فیلترها:

- **دسته‌بندی**: فیلتر بر اساس دسته‌بندی تیکت
- **نوع**: فیلتر بر اساس نوع فیلد
- **وضعیت**: فیلتر بر اساس فعال/غیرفعال

---

## انواع فیلدها

### 1. Text (متن)
- **کاربرد**: ورود متن کوتاه
- **مثال**: نام پروژه، شماره فاکتور
- **محدودیت**: حداکثر 255 کاراکتر

### 2. Textarea (متن چندخطی)
- **کاربرد**: ورود متن طولانی
- **مثال**: توضیحات اضافی، یادداشت‌ها
- **محدودیت**: بدون محدودیت (بر اساس دیتابیس)

### 3. Number (عدد)
- **کاربرد**: ورود اعداد
- **مثال**: تعداد، قیمت، درصد
- **تنظیمات**: min, max, step

### 4. Date (تاریخ)
- **کاربرد**: انتخاب تاریخ
- **مثال**: تاریخ تحویل، تاریخ شروع
- **فرمت**: YYYY-MM-DD

### 5. DateTime (تاریخ و زمان)
- **کاربرد**: انتخاب تاریخ و زمان
- **مثال**: زمان ملاقات، مهلت تحویل
- **فرمت**: YYYY-MM-DD HH:MM:SS

### 6. Boolean (بله/خیر)
- **کاربرد**: انتخاب دو حالته
- **مثال**: نیاز به تایید، فوری است؟
- **مقادیر**: true/false

### 7. Select (انتخاب تکی)
- **کاربرد**: انتخاب یک گزینه از لیست
- **مثال**: نوع درخواست، سطح اولویت
- **تنظیمات**: لیست گزینه‌ها (options)

### 8. MultiSelect (انتخاب چندتایی)
- **کاربرد**: انتخاب چند گزینه از لیست
- **مثال**: تگ‌ها، دسته‌بندی‌های اضافی
- **تنظیمات**: لیست گزینه‌ها (options)
- **ذخیره‌سازی**: مقادیر با کاما جدا می‌شوند

### 9. URL (لینک)
- **کاربرد**: ورود آدرس اینترنتی
- **مثال**: لینک مستندات، لینک پروژه
- **اعتبارسنجی**: بررسی فرمت URL

### 10. Email (ایمیل)
- **کاربرد**: ورود آدرس ایمیل
- **مثال**: ایمیل مشتری، ایمیل مسئول
- **اعتبارسنجی**: بررسی فرمت ایمیل

### 11. Phone (تلفن)
- **کاربرد**: ورود شماره تلفن
- **مثال**: شماره تماس مشتری، شماره پشتیبانی
- **اعتبارسنجی**: بررسی فرمت شماره تلفن

---

## تنظیمات پیشرفته

### Scope (محدوده اعمال):

فیلدهای سفارشی می‌توانند بر اساس:
- **Category**: فقط برای یک دسته‌بندی خاص
- **Department**: فقط برای یک دپارتمان خاص
- **Branch**: فقط برای یک شعبه خاص
- **همه**: اگر هیچکدام انتخاب نشود، برای همه اعمال می‌شود

**مثال:**
- فیلد "شماره IP" فقط برای دسته‌بندی "اینترنت"
- فیلد "مدل تجهیزات" فقط برای دسته‌بندی "تجهیزات"
- فیلد "نسخه نرم‌افزار" فقط برای دسته‌بندی "نرم‌افزار"

### Visibility (قابلیت مشاهده):

- **is_visible_to_user**: آیا کاربر عادی می‌تواند فیلد را ببیند؟
  - `true`: فیلد در پورتال کاربر نمایش داده می‌شود
  - `false`: فیلد فقط برای ادمین‌ها قابل مشاهده است

### Editability (قابلیت ویرایش):

- **is_editable_by_user**: آیا کاربر عادی می‌تواند فیلد را ویرایش کند؟
  - `true`: کاربر می‌تواند مقدار فیلد را تغییر دهد
  - `false`: فقط ادمین‌ها می‌توانند مقدار را تغییر دهند

### Required (الزامی):

- **is_required**: آیا فیلد اجباری است؟
  - `true`: کاربر باید مقدار فیلد را پر کند
  - `false`: فیلد اختیاری است

### Default Value (مقدار پیش‌فرض):

- **default_value**: مقدار پیش‌فرض برای فیلد
  - هنگام ایجاد تیکت جدید، این مقدار به صورت خودکار پر می‌شود
  - کاربر می‌تواند آن را تغییر دهد

### Help Text (متن راهنما):

- **help_text**: متن راهنمایی که زیر فیلد نمایش داده می‌شود
  - مثال: "شماره کامل پروژه را وارد کنید"

### Placeholder (متن نمونه):

- **placeholder**: متن نمونه که در فیلد نمایش داده می‌شود
  - مثال: "مثال: PRJ-2024-001"

### Display Order (ترتیب نمایش):

- **display_order**: ترتیب نمایش فیلدها در فرم
  - عدد کمتر = نمایش زودتر
  - مثال: 0 = اول، 10 = دهم

---

## API Reference

### مدیریت فیلدها (Admin Only)

#### GET `/api/custom-fields`
لیست فیلدهای سفارشی

**Query Parameters:**
- `category`: فیلتر بر اساس دسته‌بندی
- `department_id`: فیلتر بر اساس دپارتمان
- `branch_id`: فیلتر بر اساس شعبه
- `is_active`: فیلتر بر اساس وضعیت فعال
- `skip`: تعداد رکوردهای رد شده (pagination)
- `limit`: تعداد رکوردهای برگشتی

**Response:**
```json
[
  {
    "id": 1,
    "name": "project_name",
    "label": "نام پروژه",
    "field_type": "text",
    "is_required": true,
    "is_active": true,
    ...
  }
]
```

#### GET `/api/custom-fields/{field_id}`
جزئیات یک فیلد

#### POST `/api/custom-fields`
ایجاد فیلد جدید

**Request Body:**
```json
{
  "name": "project_name",
  "label": "نام پروژه",
  "field_type": "text",
  "category": "software",
  "is_required": true,
  "is_visible_to_user": true,
  "is_editable_by_user": true,
  "config": null
}
```

#### PATCH `/api/custom-fields/{field_id}`
ویرایش فیلد

#### DELETE `/api/custom-fields/{field_id}`
حذف فیلد

### مدیریت مقادیر فیلدها

#### GET `/api/custom-fields/ticket/{ticket_id}`
دریافت فیلدهای قابل اعمال برای یک تیکت با مقادیر

**Response:**
```json
[
  {
    "id": 1,
    "name": "project_name",
    "label": "نام پروژه",
    "field_type": "text",
    "value": "پروژه جدید",
    "value_id": 5,
    ...
  }
]
```

#### POST `/api/custom-fields/ticket/{ticket_id}/values`
تنظیم مقادیر فیلدها برای یک تیکت

**Request Body:**
```json
{
  "values": [
    {
      "custom_field_id": 1,
      "value": "پروژه جدید"
    },
    {
      "custom_field_id": 2,
      "value": "urgent"
    }
  ]
}
```

#### DELETE `/api/custom-fields/ticket/{ticket_id}/values/{field_id}`
حذف مقدار یک فیلد از تیکت

---

## بهترین روش‌ها

### 1. نام‌گذاری فیلدها

✅ **خوب:**
- `project_name`
- `customer_phone`
- `delivery_date`

❌ **بد:**
- `Project Name` (فاصله دارد)
- `project-name` (خط تیره بهتر است از زیرخط استفاده شود)
- `projectName` (camelCase بهتر است snake_case)

### 2. استفاده از Scope

- از فیلتر Category برای محدود کردن فیلدها به دسته‌بندی‌های خاص استفاده کنید
- از فیلتر Department برای فیلدهای خاص دپارتمان استفاده کنید
- از فیلتر Branch برای فیلدهای خاص شعبه استفاده کنید

### 3. تنظیمات Visibility و Editability

- فیلدهای حساس را فقط برای ادمین‌ها قابل مشاهده کنید
- فیلدهای اطلاعاتی را برای کاربران قابل مشاهده اما غیرقابل ویرایش کنید
- فیلدهای عمومی را هم قابل مشاهده و هم قابل ویرایش کنید

### 4. استفاده از Default Value

- برای فیلدهای با مقدار ثابت، از Default Value استفاده کنید
- این کار سرعت ایجاد تیکت را افزایش می‌دهد

### 5. Help Text و Placeholder

- از Help Text برای راهنمایی کاربر استفاده کنید
- از Placeholder برای نمایش مثال استفاده کنید

### 6. ترتیب نمایش

- فیلدهای مهم را با display_order کمتر تنظیم کنید
- فیلدهای اختیاری را در انتها قرار دهید

### 7. Config برای SELECT/MULTISELECT

- گزینه‌ها را به صورت واضح و قابل فهم تعریف کنید
- از برچسب‌های فارسی استفاده کنید
- مقدار (value) را به انگلیسی و کوتاه نگه دارید

### 8. اعتبارسنجی

- برای NUMBER، محدودیت‌های min/max را تنظیم کنید
- برای TEXT، از pattern در config استفاده کنید (در آینده)

---

## مثال‌های کاربردی

### مثال 1: فیلد "نام پروژه" برای تیکت‌های نرم‌افزار

```json
{
  "name": "project_name",
  "label": "نام پروژه",
  "label_en": "Project Name",
  "field_type": "text",
  "category": "software",
  "is_required": true,
  "is_visible_to_user": true,
  "is_editable_by_user": true,
  "placeholder": "مثال: PRJ-2024-001",
  "help_text": "نام کامل پروژه را وارد کنید",
  "display_order": 1
}
```

### مثال 2: فیلد "اولویت درخواست" با Select

```json
{
  "name": "request_priority",
  "label": "اولویت درخواست",
  "field_type": "select",
  "category": null,
  "is_required": false,
  "is_visible_to_user": true,
  "is_editable_by_user": true,
  "config": {
    "options": [
      {"value": "urgent", "label": "فوری"},
      {"value": "high", "label": "بالا"},
      {"value": "normal", "label": "عادی"},
      {"value": "low", "label": "پایین"}
    ]
  },
  "display_order": 2
}
```

### مثال 3: فیلد "تاریخ تحویل" فقط برای دپارتمان IT

```json
{
  "name": "delivery_date",
  "label": "تاریخ تحویل",
  "field_type": "date",
  "department_id": 1,
  "is_required": false,
  "is_visible_to_user": true,
  "is_editable_by_user": false,
  "display_order": 10
}
```

---

## عیب‌یابی

### مشکل: فیلدها در فرم نمایش داده نمی‌شوند

**راه‌حل:**
1. بررسی کنید فیلد `is_active` برابر `true` باشد
2. بررسی کنید فیلد `is_visible_to_user` برابر `true` باشد
3. بررسی کنید Scope فیلد با تیکت مطابقت داشته باشد
4. بررسی کنید دسته‌بندی تیکت با فیلد مطابقت داشته باشد

### مشکل: فیلدها قابل ویرایش نیستند

**راه‌حل:**
1. بررسی کنید فیلد `is_editable_by_user` برابر `true` باشد
2. بررسی کنید نقش کاربر (فقط ادمین‌ها می‌توانند همه فیلدها را ویرایش کنند)

### مشکل: مقادیر ذخیره نمی‌شوند

**راه‌حل:**
1. بررسی کنید اعتبارسنجی فیلد (مثلاً برای NUMBER، min/max)
2. بررسی کنید فیلدهای Required پر شده باشند
3. بررسی کنید لاگ‌های سرور برای خطاها

---

## پشتیبانی

برای سوالات و مشکلات:
- 📧 ایمیل: support@iranmehr.com
- 📚 مستندات: [docs/CUSTOM_FIELDS.md](CUSTOM_FIELDS.md)
- 🐛 Issues: [GitHub Issues](https://github.com/your-repo/issues)

---

**آخرین به‌روزرسانی:** 2025-01-23  
**نسخه:** 1.0.0


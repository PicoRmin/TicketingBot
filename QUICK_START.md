# راهنمای سریع شروع / Quick Start Guide

## 🚀 شروع سریع در ۵ دقیقه

### مرحله ۱: ایجاد فایل .env
```bash
copy env.example .env
```

### مرحله ۲: تولید SECRET_KEY

**روش ۱: استفاده از اسکریپت (توصیه می‌شود)**
```bash
python scripts/generate_secret_key.py
```
خروجی چیزی شبیه این خواهد بود:
```
Generated SECRET_KEY: xK9mP2vQ7wR5tY8uI3oP6aS9dF1gH4jK7lM0nQ2rT5vW8xY1zA4bC6dE9fG
```
این کلید را کپی کنید.

**روش ۲: استفاده از Python**
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### مرحله ۳: ویرایش فایل .env

فایل `.env` را با یک ویرایشگر متن باز کنید و مقادیر زیر را تنظیم کنید:

```env
# SECRET_KEY را با کلید تولید شده جایگزین کنید
SECRET_KEY=xK9mP2vQ7wR5tY8uI3oP6aS9dF1gH4jK7lM0nQ2rT5vW8xY1zA4bC6dE9fG

# TELEGRAM_BOT_TOKEN را می‌توانید بعداً تنظیم کنید
TELEGRAM_BOT_TOKEN=
```

### مرحله ۴: دریافت توکن ربات تلگرام (اختیاری - برای فاز بعدی)

1. در تلگرام به `@BotFather` بروید
2. دستور `/newbot` را ارسال کنید
3. نام ربات را وارد کنید (مثلاً: `Iranmehr Ticketing Bot`)
4. Username ربات را وارد کنید (باید به `bot` ختم شود، مثلاً: `iranmehr_ticketing_bot`)
5. توکن دریافتی را در فایل `.env` قرار دهید:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890
```

📖 برای راهنمای کامل، فایل `TELEGRAM_BOT_SETUP.md` را مطالعه کنید.

### مرحله ۵: تست

```bash
# فعال‌سازی Virtual Environment
.venv\Scripts\activate

# اجرای برنامه
uvicorn app.main:app --reload
```

سپس در مرورگر به آدرس زیر بروید:
- http://localhost:8000/docs

---

## ✅ چک‌لیست

- [ ] فایل `.env` ایجاد شده
- [ ] `SECRET_KEY` تنظیم شده
- [ ] `TELEGRAM_BOT_TOKEN` تنظیم شده (اختیاری)
- [ ] برنامه با موفقیت اجرا می‌شود
- [ ] می‌توانید به `/docs` دسترسی داشته باشید

---

## ❓ سوالات متداول

**سوال**: آیا می‌توانم بدون TELEGRAM_BOT_TOKEN شروع کنم؟
**جواب**: بله! می‌توانید برنامه را بدون توکن ربات اجرا کنید. فقط برای فاز ۶ (ربات تلگرام) به آن نیاز دارید.

**سوال**: SECRET_KEY چیست و چرا مهم است؟
**جواب**: SECRET_KEY برای رمزگذاری JWT Tokens و سایر عملیات امنیتی استفاده می‌شود. باید یک رشته تصادفی و قوی باشد.

**سوال**: اگر SECRET_KEY را فراموش کنم چه می‌شود؟
**جواب**: می‌توانید دوباره یک کلید جدید تولید کنید. فقط توجه داشته باشید که اگر کاربران قبلاً وارد شده باشند، باید دوباره وارد شوند.


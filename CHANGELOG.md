# Changelog - سیستم تیکتینگ ایرانمهر

## [Unreleased] - 2025-01-26

### ✅ اضافه شده (Added)

#### Telegram Bot - EPIC 2 تکمیل شده (Advanced Ticketing) ⭐
- **مشاهده جزئیات کامل تیکت (`/ticket <number>`)**:
  - نمایش کامل جزئیات تیکت با تاریخچه، کامنت‌ها و فایل‌ها
  - Inline keyboard برای actions مختلف (پاسخ، کامنت، تاریخچه، فایل‌ها)
  - پشتیبانی از pagination برای کامنت‌ها
  - نمایش اولویت و کارشناس مسئول
  - دسترسی‌های نقش‌محور برای actions

- **پاسخ و کامنت روی تیکت (`/reply <number>`)**:
  - امکان پاسخ به تیکت با کامنت
  - امکان attach فایل به کامنت
  - انتخاب internal/external برای کامنت (برای مدیران)
  - اعتبارسنجی فایل (نوع، حجم، تعداد)

- **فیلتر و جستجوی پیشرفته تیکت‌ها (`/search`)**:
  - فیلتر بر اساس وضعیت (pending, in_progress, resolved, closed)
  - فیلتر بر اساس اولویت (critical, high, medium, low)
  - فیلتر بر اساس دسته‌بندی
  - فیلتر بر اساس تاریخ (امروز، هفته گذشته، ماه گذشته، همه)
  - جستجوی متنی در عنوان و توضیحات
  - امکان ترکیب چند فیلتر با هم
  - نمایش خلاصه فیلترهای فعال
  - Pagination برای نتایج

- **مدیریت اولویت تیکت (`/priority <number>`)**:
  - تغییر اولویت تیکت از طریق بات
  - بررسی دسترسی مدیر قبل از تغییر
  - نمایش اولویت فعلی قبل از انتخاب
  - استفاده از inline keyboard برای انتخاب

- **تخصیص تیکت (`/assign <number>`)**:
  - تخصیص تیکت به agent از طریق بات
  - نمایش لیست agents با workload
  - نمایش workload با emoji و رنگ‌بندی (🟢 < 5, 🟡 < 10, 🔴 >= 10)
  - مرتب‌سازی agents بر اساس workload
  - جستجوی agent بر اساس نام
  - فیلتر بر اساس شعبه برای branch_admin

- **عملیات دسته‌ای تیکت‌ها (`/bulk`)**:
  - انتخاب چند تیکت با inline keyboard toggle
  - Actions: status, assign, unassign, delete
  - برای status: انتخاب status جدید
  - برای assign: انتخاب agent
  - نمایش تعداد تیکت‌های انتخاب شده
  - نمایش نتیجه با جزئیات (موفق/ناموفق)

- **بهبودهای کلی**:
  - پشتیبانی کامل i18n (فارسی و انگلیسی)
  - مدیریت خطا و cleanup مناسب
  - Integration کامل با API
  - Callback handlers برای دسترسی سریع

### 🔧 بهبود یافته (Improved)
- بهبود UX در تمام handlers
- بهبود مدیریت خطا
- بهبود پیام‌های کاربری

## [Unreleased] - 2025-01-23

### ✅ اضافه شده (Added)

#### Email Notifications (اطلاع‌رسانی ایمیل) - ✨ ویژگی پیشرفته و حرفه‌ای
- **سرویس ارسال ایمیل پیشرفته**:
  - کلاس `EmailService` برای مدیریت کامل ارسال ایمیل
  - پشتیبانی از SMTP با TLS/SSL
  - ارسال غیرهمزمان (Async) با `aiosmtplib`
  - پشتیبانی از فایل‌های پیوست
  - پشتیبانی از CC و BCC
  - مدیریت خطا و Retry خودکار
  - Logging کامل برای عیب‌یابی

- **قالب‌های HTML حرفه‌ای**:
  - قالب‌های Responsive و زیبا
  - پشتیبانی از چندزبانه (فارسی و انگلیسی)
  - استفاده از Jinja2 برای رندرینگ
  - استایل‌های CSS مدرن با گرادیان‌ها
  - Badge‌های رنگی برای وضعیت‌ها و اولویت‌ها

- **انواع ایمیل‌های خودکار**:
  - ایمیل ایجاد تیکت (`ticket_created`)
  - ایمیل تغییر وضعیت (`ticket_status_changed`)
  - ایمیل تخصیص تیکت (`ticket_assigned`)
  - ایمیل افزودن کامنت (`comment_added`)
  - ایمیل هشدار SLA (`sla_warning`)
  - ایمیل نقض SLA (`sla_breach`)

- **یکپارچه‌سازی کامل**:
  - یکپارچه‌سازی با `notification_service`
  - یکپارچه‌سازی با `sla_alert_service`
  - یکپارچه‌سازی با `comment_service`
  - یکپارچه‌سازی با `ticket_service` (تخصیص)

- **Migration v17**:
  - اضافه شدن فیلد `email` به جدول `users`
  - ایجاد Index برای فیلد email

- **مستندات کامل**:
  - راهنمای کامل در `docs/EMAIL_NOTIFICATIONS.md`
  - راهنمای تنظیمات SMTP برای سرویس‌های مختلف
  - راهنمای تست و عیب‌یابی
  - بهترین روش‌ها و نکات امنیتی

#### Custom Fields (فیلدهای سفارشی) - ✨ ویژگی پیشرفته
- **مدل‌های دیتابیس**:
  - مدل `CustomField` برای تعریف فیلدهای سفارشی
  - مدل `TicketCustomFieldValue` برای ذخیره مقادیر فیلدها
  - پشتیبانی از 10 نوع فیلد: TEXT, TEXTAREA, NUMBER, DATE, DATETIME, BOOLEAN, SELECT, MULTISELECT, URL, EMAIL, PHONE
  - فیلتر بر اساس Category, Department, Branch
  - تنظیمات پیشرفته: Required, Visible, Editable, Default Value, Help Text, Placeholder
  - Migration v16 برای ایجاد جداول

- **Service Layer**:
  - مدیریت کامل CRUD برای Custom Fields
  - دریافت فیلدهای قابل اعمال برای هر تیکت
  - مدیریت مقادیر فیلدها برای تیکت‌ها
  - اعتبارسنجی پیشرفته مقادیر بر اساس نوع و config
  - پشتیبانی از فیلترهای پیشرفته

- **API Endpoints**:
  - `GET /api/custom-fields` - لیست فیلدها
  - `GET /api/custom-fields/{field_id}` - جزئیات فیلد
  - `POST /api/custom-fields` - ایجاد فیلد جدید
  - `PATCH /api/custom-fields/{field_id}` - به‌روزرسانی فیلد
  - `DELETE /api/custom-fields/{field_id}` - حذف فیلد
  - `GET /api/custom-fields/ticket/{ticket_id}` - فیلدهای قابل اعمال برای تیکت با مقادیر
  - `POST /api/custom-fields/ticket/{ticket_id}/values` - تنظیم مقادیر
  - `DELETE /api/custom-fields/ticket/{ticket_id}/values/{field_id}` - حذف مقدار

- **تست‌ها**:
  - 18 تست Unit برای Service Layer
  - 9 تست Integration برای API Endpoints
  - پوشش کامل: ایجاد، خواندن، به‌روزرسانی، حذف، اعتبارسنجی
  - تست‌های اعتبارسنجی برای همه انواع فیلدها

- **Frontend (تکمیل شده)**:
  - صفحه مدیریت Custom Fields (`CustomFields.tsx`)
    - لیست کامل فیلدها با فیلترهای پیشرفته
    - فرم ایجاد/ویرایش فیلد با تمام تنظیمات
    - پشتیبانی از Config برای SELECT/MULTISELECT و NUMBER
    - مدیریت گزینه‌های Select با افزودن/حذف پویا
    - اعتبارسنجی کامل فرم
  - کامپوننت رندر فیلدها (`CustomFieldRenderer.tsx`)
    - رندر خودکار بر اساس نوع فیلد
    - پشتیبانی از همه 10 نوع فیلد
    - حالت خواندنی و قابل ویرایش
    - اعتبارسنجی سمت کلاینت
    - نمایش زیبا برای مقادیر
  - یکپارچه‌سازی با TicketDetail
    - نمایش فیلدهای سفارشی در جزئیات تیکت
    - امکان ویرایش و ذخیره مقادیر
    - مرتب‌سازی بر اساس display_order
  - یکپارچه‌سازی با فرم ایجاد تیکت (UserPortal)
    - بارگذاری خودکار فیلدها بر اساس دسته‌بندی
    - ذخیره خودکار مقادیر پس از ایجاد تیکت
    - فیلتر فیلدهای قابل مشاهده برای کاربر
  - یکپارچه‌سازی با UserTicketDetail
    - نمایش فیلدهای سفارشی (فقط خواندنی)
    - فیلتر فیلدهای قابل مشاهده
  - Navigation و Routing
    - اضافه شدن لینک "فیلدهای سفارشی" به منوی ادمین
    - Route جدید `/custom-fields`

#### بهبود Dashboard
- فیلترهای پیشرفته (تاریخ، شعبه، دپارتمان، اولویت)
- Export PDF با ReportLab
- نمودارهای جذاب: Bar, Pie, Area, Radar
- UI/UX بهبود یافته با انیمیشن‌ها

#### Production Setup (تکمیل شده) ✅
- **راهنمای کامل Production Setup** (`docs/PRODUCTION_SETUP.md`):
  - راهنمای نصب و راه‌اندازی کامل
  - تنظیمات امنیتی
  - تنظیمات Database (PostgreSQL و SQLite)
  - تنظیمات Web Server (Nginx)
  - Windows Service Setup
  - Backup Strategy
  - Monitoring و Logging
  - Performance Optimization
  - Troubleshooting
  - Checklist استقرار
- **اسکریپت‌های استقرار**:
  - `scripts/backup.sh` - Backup خودکار (Linux)
  - `scripts/backup.bat` - Backup خودکار (Windows)
  - `scripts/start_production.sh` - راه‌اندازی Production (Linux)
  - `scripts/check_production.sh` - بررسی وضعیت Production
  - `scripts/setup_production.py` - راه‌اندازی خودکار Production
  - `scripts/ticketing.service` - systemd service file
  - `scripts/install_windows_service.bat` - نصب Windows Service
  - `scripts/uninstall_windows_service.bat` - حذف Windows Service
- **راهنمای Monitoring** (`docs/MONITORING.md`):
  - Health Check
  - Logging
  - Monitoring Tools
  - Alerting
  - Performance Monitoring
- **به‌روزرسانی config.py**: اضافه شدن aliases برای Email settings
- **به‌روزرسانی env.example**: اضافه شدن تنظیمات Email

#### End-to-End Tests (تکمیل شده) ✅
- **تست‌های خودکار** (`tests/test_e2e.py`):
  - سناریوی کامل گردش تیکت (ایجاد، تخصیص، تایم‌لاگ، تاریخچه)
  - تست APIClient ربات تلگرام (ورود، دریافت شعب، ثبت تیکت)
- **مستندات**:
  - `docs/END_TO_END_TESTS.md` – سناریوها، اجرای تست‌ها و چک‌لیست دستی
  - به‌روزرسانی `docs/TESTING.md` و README

#### Security Tests (تکمیل شده) ✅
- **تست‌های امنیتی API** (`tests/test_security_api.py`):
  - ورود نامعتبر و دسترسی بدون توکن → 401
  - جلوگیری از دسترسی کاربر عادی به Endpoint‌های ادمین → 403
  - جلوگیری از مشاهده تیکت سایر کاربران
  - اعتبارسنجی enum و payloadهای مشکوک (SQL-like)
- **ادغام در مستندات**: `docs/TESTING.md` (بخش جدید Security Tests)، README، Remaining Work

#### Performance Tests (جدید) ✅
- اسکریپت مستقل `tests/performance/run_performance_tests.py` برای Load/Stress (با پارامترهای concurrency و مدت‌زمان)
- مستندات جدید: `docs/PERFORMANCE_TESTS.md`
- ادغام در `docs/TESTING.md` و README

#### Frontend Hardening & i18n ✅
- ProtectedRoute سطح اپلیکیشن + کنترل نقش برای تمام مسیرها
- زبان‌بندی کامل با `i18next` (fa/en) و ذخیره انتخاب کاربر
- Global Error Toast متصل به error bus مشترک
- بهبود `api.ts`: مدیریت خطای 401، خروج خودکار، پیام‌های شبکه
- افزودن Language Switcher و ترجمه ناوبری/دکمه‌ها

#### Integration Tests (تکمیل شده) ✅
- **تست‌های API Endpoints** (`test_integration_api.py`):
  - 9 کلاس تست برای تمام API endpoints
  - بیش از 30 تست یکپارچه‌سازی
  - پوشش کامل Authentication, Authorization, CRUD operations
  - تست‌های: Authentication, Tickets, Comments, Branches, Departments, SLA, Reports, Custom Fields, Time Tracker
- **تست‌های Database Operations** (`test_integration_database.py`):
  - 4 کلاس تست برای عملیات دیتابیس
  - بیش از 15 تست یکپارچه‌سازی
  - پوشش کامل Transactions, Relationships, Constraints, Cascades
- **تست‌های File Operations** (`test_integration_files.py`):
  - 5 کلاس تست برای عملیات فایل
  - بیش از 15 تست یکپارچه‌سازی
  - پوشش کامل Upload, Download, Delete, Validation
- **مستندات**: راهنمای جامع در `docs/INTEGRATION_TESTS.md`

#### Unit Tests (تکمیل شده) ✅
- **تست‌های Models** (`test_models_complete.py`):
  - 9 کلاس تست برای تمام مدل‌ها (User, Ticket, SLA, CustomField, Comment, TimeLog, AutomationRule, BranchInfrastructure, SystemSettings)
  - بیش از 30 تست واحد
  - پوشش کامل روابط و فیلدها
- **تست‌های Services** (`test_services_complete.py`):
  - 8 کلاس تست برای تمام سرویس‌ها (Ticket, User, Branch, Department, Comment, SLA, CustomField, TimeTracker)
  - بیش از 40 تست واحد
  - پوشش کامل توابع اصلی
- **تنظیمات pytest** (`pytest.ini`):
  - تنظیمات کامل pytest
  - Coverage reporting (هدف: 70%+)
  - Markers برای دسته‌بندی تست‌ها
- **مستندات تست‌ها**:
  - `tests/README.md`: راهنمای کامل تست‌ها
  - `docs/TESTING.md`: راهنمای جامع تست‌ها
- **افزودن pytest به requirements.txt**

#### صفحه مدیریت SLA (تکمیل شده) ✅
- **مدیریت قوانین SLA**:
  - لیست کامل قوانین با فیلتر وضعیت
  - فرم ایجاد/ویرایش قانون با تمام تنظیمات
  - حذف و فعال/غیرفعال کردن قوانین
  - اعتبارسنجی کامل فرم
- **نمایش لاگ‌های SLA**:
  - لیست کامل لاگ‌ها با فیلترهای پیشرفته
  - فیلتر بر اساس وضعیت پاسخ، وضعیت حل، Escalation
  - Pagination برای لاگ‌ها
  - لینک مستقیم به تیکت‌ها
  - نمایش اطلاعات کامل: مهلت‌ها، زمان‌های واقعی، وضعیت‌ها
- **آمار و نمودارهای SLA**:
  - 8 کارت آماری: کل لاگ‌ها، پاسخ در مهلت، هشدار پاسخ، نقض پاسخ، حل در مهلت، هشدار حل، نقض حل، Escalated
  - نمودار Pie برای وضعیت پاسخ
  - نمودار Pie برای وضعیت حل
  - نمودار Bar مقایسه‌ای پاسخ و حل
  - استفاده از Recharts برای نمودارهای زیبا
- **Backend API**:
  - تابع `list_sla_logs` در `sla_service.py`
  - API endpoint `/api/sla/logs` برای لیست لاگ‌ها
  - پشتیبانی از فیلترهای پیشرفته
  - اضافه شدن `ticket_number` و `sla_rule_name` به `SLALogResponse`
- **مستندات کامل**: راهنمای جامع در `docs/SLA_MANAGEMENT.md`

#### بهبود فیلترهای صفحه Tickets
- فیلتر شماره تیکت
- فیلتر تاریخ (از/تا)
- فیلتر کارشناس مسئول
- فیلتر کاربر ایجادکننده
- پنل فیلترهای پیشرفته با قابلیت باز/بسته شدن

#### Unit Tests & Security Tests
- تست‌های Models
- تست‌های Services
- تست‌های API Endpoints
- تست‌های Security (Authentication, Authorization, Input Validation, SQL Injection, XSS)
- تنظیمات pytest با fixtures

#### Production Setup
- اعتبارسنجی تنظیمات Production
- بهبود Health Check endpoint
- مستندات کامل استقرار (`DEPLOYMENT.md`)
- راهنمای PostgreSQL, Nginx, SSL, Backup

#### بهبود پنل کارشناس
- **Time Tracker (زمان‌سنج کار)**: 
  - مدل `TimeLog` برای ثبت زمان کار روی تیکت‌ها
  - API endpoints برای شروع/توقف تایمر
  - نمایش تایمر فعال در صفحه جزئیات تیکت
  - لیست تاریخچه زمان کار
  - نمایش خلاصه کل زمان کار
  - Migration v15 برای ایجاد جدول `time_logs`

- **Bulk Actions (عملیات گروهی)**:
  - API endpoint برای عملیات گروهی روی چند تیکت
  - پشتیبانی از تغییر وضعیت گروهی
  - پشتیبانی از تخصیص گروهی
  - پشتیبانی از حذف تخصیص گروهی
  - پشتیبانی از حذف گروهی
  - UI با checkbox و modal برای انتخاب عملیات

- **Quick Actions (عملیات سریع)**:
  - دکمه‌های سریع در لیست تیکت‌ها برای تغییر وضعیت
  - ▶️ شروع کار (تغییر به in_progress)
  - ✅ حل شده (تغییر به resolved)
  - 🔒 بستن (تغییر به closed)

#### پورتال کاربران
- صفحه `UserPortal` برای ایجاد و مشاهده تیکت‌های کاربر
- صفحه `UserTicketDetail` برای مشاهده جزئیات تیکت
- صفحه `UserDashboard` برای نمایش آمار تیکت‌های کاربر
- ریدایرکت خودکار کاربران عادی به پورتال کاربر

#### رفع خطاها
- رفع خطای TypeScript در `App.tsx` (نوع `fetchProfile`)
- رفع خطای import در `time_tracker.py`

### 🔧 تغییر یافته (Changed)

- بهبود `TicketDetail.tsx` با اضافه کردن Time Tracker
- بهبود `Tickets.tsx` با اضافه کردن Bulk Actions و Quick Actions
- بهبود `App.tsx` برای ریدایرکت کاربران عادی
- اضافه شدن route‌های جدید در `main.tsx`
- بهبود `Dashboard.tsx` با نمودارها و فیلترهای پیشرفته
- بهبود `config.py` با اعتبارسنجی Production
- بهبود `main.py` با Health Check پیشرفته

### 📝 مستندات

- به‌روزرسانی `PROJECT_STATUS_UPDATE.md`
- به‌روزرسانی `REMAINING_WORK.md`
- ایجاد `CHANGELOG.md`

---

## [Previous Versions]

برای مشاهده تغییرات نسخه‌های قبلی، به commit history مراجعه کنید.


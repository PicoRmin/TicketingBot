# 📋 فهرست کامل ورودی‌هایی که باید تهیه شوند

این فایل توضیح دقیق هر موردی است که برای تکمیل مرحله Hardening و ادامه توسعه لازم داریم. لطفاً برای هر آیتم اطلاعات خواسته‌شده را آماده کنید تا در `docs/CONFIG_INPUTS_CHECKLIST.md` و تنظیمات پروژه ثبت شود.

---

## 1. دامنه و DNS
- دامنه‌های نهایی برای API، پنل ادمین و پورتال مشتری (مثلاً `api.iranmehr.com`, `admin.iranmehr.com`, `portal.iranmehr.com`).
- Registrar و DNS Provider (Cloudflare, Route53, ...).
- رکوردهای موردنیاز (A یا CNAME)، IPهای مقصد، TTL پیشنهادی و هرگونه Failover.
- در صورت استفاده از subdomain wildcard یا split horizon، جزئیات را ذکر کنید.

## 2. SSL / Let’s Encrypt
- ایمیل تماس برای Let’s Encrypt و زمان‌بندی تمدید.
- مسیر ذخیره گواهی‌ها روی سرور و مجوزهای دسترسی.
- در صورت نیاز به گواهی سازمانی (EV/OV)، مشخصات صادرکننده و فرآیند نصب.
- برای Webhook یا سرویس‌های خارجی اگر گواهی جدا لازم است، آن را نیز مشخص کنید.

## 3. Reverse Proxy و Nginx
- سیستم‌عامل و نسخه Nginx روی سرور Production.
- حساب کاربری که سرویس با آن اجرا می‌شود (`www-data`, `ticketing`, ...).
- مسیرهای موردنظر برای static files / media و محدودیت دسترسی (ProtectSystem, ProtectHome).
- سیاست Rate Limiting یا WAF در لایه Nginx (در صورت وجود).

## 4. CI/CD Pipeline
- مخزن اصلی (GitHub/GitLab) و شاخه‌هایی که باید build/deploy شوند.
- محیط‌های هدف (staging, production) و نوع استقرار (manual approval, auto deploy).
- Runner یا GitHub Actions self-hosted در دسترس، الزامات دسترسی، و محدودیت شبکه.

## 5. CI/CD Secrets
- توکن دسترسی به مخزن/Registry، کلید SSH برای سرور، و تمامی secrets لازم (DATABASE_URL, SMTP, TELEGRAM, REDIS).
- سیاست چرخش و محل امن نگهداری secrets (GitHub Secrets, Vault, ...).

## 6. سرور Production
- IP یا Hostname، سیستم‌عامل (Ubuntu 22.04, Windows Server 2019, ...)، دسترسی SSH/RDP، و کاربر مجاز برای استقرار.
- سیاست Backup/Snapshot سطح سرور و مسئولیت مدیریت آن.
- محدودیت‌های فایروال (پورت‌های باز، دسترسی مبدأ) و نیازمندی‌های شبکه خصوصی (VPN, Bastion).

## 7. پایگاه‌داده
- نوع پایگاه داده (PostgreSQL توصیه شده)، نسخه، هاست/پورت، کاربر/پسورد.
- آیا نیاز به High Availability، replication یا managed service (RDS, Azure DB) دارید؟
- سیاست Backup، retention و فرآیند بازیابی دیتابیس.

## 8. SMTP / Email
- سرویس‌دهنده (Gmail, Outlook, Mailgun, ...)، پورت و پروتکل (TLS/SSL).
- حساب کاربری و رمز یا App Password.
- آدرس‌های Reply-To، BCC و اطلاعات DMARC/DKIM/SPF در صورت موجود بودن.
- محدودیت ارسال (Rate limits) یا نیاز به Queueing.

## 9. Telegram Bot
- توکن رسمی ربات.
- در صورت استفاده از Webhook: دامنه مقصد، مسیر، secret و محدوده IP مجاز.
- سیاست‌های امنیتی (Rate limit, Allowed updates, Logging).

## 10. کانال‌های اعلان (Notification Channels)
- Slack/Microsoft Teams یا هر webhook خارجی که باید اعلان دریافت کند.
- آستانه هشدار (مثلاً تعداد خطای متوالی یا SLA breach).

## 11. Knowledge Base
- ساختار مقالات (موضوعات، دسته‌بندی‌ها، تگ‌ها).
- زبان‌های پشتیبانی شده و نقش‌هایی که مجاز به مشاهده/ویرایش هستند.
- سیاست انتشار و workflow تأیید محتوا.

## 12. Real-time / Webhook Integrations
- فهرست مشتریان یا سرویس‌هایی که باید webhook دریافت کنند، همراه با URL و Secret.
- نوع رویدادهایی که باید ارسال شود (Ticket created, SLA breached, ...).
- سیاست Retry، backoff و Dead-letter (در صورت نیاز).

## 13. CRM Integration
- سیستم مقصد (Microsoft Dynamics, Zoho, HubSpot, ...)، نسخه و API endpoints.
- API Keys یا OAuth credentials و scope دسترسی.
- mapping داده‌ها (Field mapping بین Ticketing و CRM) و فرکانس همگام‌سازی.

## 14. اهداف Performance / SLA
- تعداد کاربر/کارشناس همزمان که باید پشتیبانی شوند.
- حداکثر تعداد تیکت در روز و SLA پاسخ/حل (مثلاً پاسخ < 1 دقیقه، حل < 4 ساعت).
- محدودیت منابع سرور (CPU/RAM) یا بودجه عملیاتی.

## 15. سیاست‌های امنیتی
- الزامات 2FA برای نقش‌های حساس (Admin/Central Admin).
- نیاز به Audit Logging سطح بالا (چه رویدادهایی باید ثبت شوند و retention آنها).
- سیاست چرخش رمزها، مدت اعتبار توکن‌ها و Least Privilege برای نقش‌ها.

## 16. Redis / Cache
- آیا Redis برای Session/Cache/Rate Limit نیاز است؟ اگر بله، آدرس، پورت و احراز هویت آن.
- تصمیم درباره ذخیره‌سازی نشست ربات تلگرام یا اطلاعات موقت در Redis.

## 17. Monitoring Stack
- انتخاب ابزار (Prometheus + Grafana، Loki/ELK، Datadog، ...).
- نیاز به dashboards آماده، Alert rules و مقصد هشدارها.
- retention داده‌های مانیتورینگ و محل نصب Agentها.

## 18. Backup & Disaster Recovery
- مقصد نهایی Backup (Local, S3, NAS) و میزان تکرار (روزانه/ساعتی).
- مدت نگهداری فراتر از مقدار پیش‌فرض (7 روز) و تست دوره‌ای بازیابی.
- سناریوهای Disaster Recovery (RPO/RTO هدف و فرآیند اجرایی).

## 19. Documentation & Training
- زبان و فرمت موردنظر برای راهنماها (PDF, Wiki, Markdown).
- سبک ویدیوهای آموزشی (اسکرین‌کست، انیمیشن، دوبله) و کانال انتشار (اینترانت، LMS).
- افراد یا تیم‌هایی که باید نسخه نهایی را دریافت و تأیید کنند.

---

هر زمان اطلاعات هر بخش آماده شد، لطفاً در همین فایل یا مستقیماً در `docs/CONFIG_INPUTS_CHECKLIST.md` وارد کنید تا پیگیری و به‌روزرسانی‌ها ادامه پیدا کند.‬


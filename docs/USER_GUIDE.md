## راهنمای کاربران (User Portal)

این سند نحوه استفاده کاربران نهایی از پرتال تیکت ایرانمهر را توضیح می‌دهد.

> **📱 برای راهنمای استفاده از ربات تلگرام**: به [راهنمای کاربران تلگرام](./TELEGRAM_USER_GUIDE.md) مراجعه کنید.

---

### 1. ورود به سیستم

1. آدرس Frontend (معمولاً `http://localhost:5173`) را باز کنید.
2. روی «🔐 ورود» کلیک کنید.
3. نام کاربری و رمز عبور خود را وارد کنید.
4. در صورت فراموشی رمز، با پشتیبانی تماس بگیرید؛ امکان بازیابی از طریق ایمیل/تلگرام نیز در آینده اضافه خواهد شد.

---

### 2. داشبورد کاربر

بعد از ورود، به صورت خودکار به `/user-dashboard` هدایت می‌شوید:

- کارت خلاصه وضعیت تیکت‌ها (باز، در حال پردازش، بسته شده).
- آخرین تیکت‌های ثبت‌شده و وضعیت SLA آن‌ها.
- دکمه سریع «ثبت تیکت جدید».

نکته: با انتخاب زبان (فا/En) در منوی بالا، کل رابط کاربری دو زبانه می‌شود.

---

### 3. ثبت تیکت جدید

1. به مسیر `/user-portal` بروید.
2. فرم «ایجاد تیکت» شامل فیلدهای زیر است:
   - عنوان تیکت
   - توضیحات
   - شعبه/دپارتمان (بر اساس نقش و سطح دسترسی شما)
   - اولویت و دسته‌بندی
   - فیلدهای سفارشی: برای هر دسته، فیلدهای مخصوص (تاریخ، عددی، انتخابی و ...) نمایش داده می‌شود.
3. در صورت نیاز فایل پیوست کنید (حداکثر 10 مگابایت).
4. دکمه «ثبت تیکت» را بزنید؛ پس از موفقیت، شماره تیکت و وضعیت اولیه نمایش داده می‌شود.

---

### 4. مشاهده و پیگیری تیکت

- لیست تمام تیکت‌ها در همان صفحه `User Portal` موجود است (قابلیت جست‌وجو و فیلتر).
- با کلیک روی هر تیکت، صفحه جزئیات (`/user-tickets/:id`) باز می‌شود:
  - وضعیت فعلی و تاریخچه وضعیت‌ها
  - SLA (زمان پاسخ/حل)
  - فیلدهای سفارشی (فقط خواندنی)
  - لیست کامنت‌ها و پیوست‌ها
- می‌توانید از همین صفحه کامنت جدید ثبت کنید یا فایل اضافه کنید.

---

### 5. اعلان‌ها

- اعلان‌های ایمیلی/تلگرامی بعد از هر تغییر وضعیت یا پاسخ جدید ارسال می‌شود (در صورت فعال بودن تنظیمات کاربر).
- نسخه وب نیز با Error Toast جهانی، خطاهای شبکه یا انقضای نشست را نمایش می‌دهد.

---

### 6. مدیریت زبان و تم

- دکمه «فا/En» در هدر برای تغییر زبان.
- دکمه «تاریک/روشن» برای تغییر تم.
- تنظیمات انتخابی در LocalStorage ذخیره می‌شود تا در بازدید بعدی حفظ شود.

---

### 7. نکات امنیتی

- پس از پایان کار، حتماً روی «🚪 خروج» کلیک کنید.
- رمز عبور را به صورت دوره‌ای تغییر دهید.
- از اشتراک‌گذاری توکن یا اطلاعات ورود خودداری کنید.

---

---

## مستندات مرتبط

- [راهنمای کاربران تلگرام](./TELEGRAM_USER_GUIDE.md) - راهنمای کامل استفاده از ربات تلگرام
- [راهنمای کامل نقش‌ها](./ROLES_GUIDE.md) - راهنمای کامل تمام نقش‌ها و دسترسی‌ها

---

**آخرین به‌روزرسانی:** 2025-01-25


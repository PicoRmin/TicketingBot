# ⏱️ راهنمای کامل مدیریت SLA

## فهرست مطالب
1. [معرفی](#معرفی)
2. [ویژگی‌ها](#ویژگی‌ها)
3. [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
4. [استفاده از Frontend](#استفاده-از-frontend)
5. [مدیریت قوانین SLA](#مدیریت-قوانین-sla)
6. [نمایش لاگ‌های SLA](#نمایش-لاگ‌های-sla)
7. [آمار و نمودارها](#آمار-و-نمودارها)
8. [API Reference](#api-reference)
9. [بهترین روش‌ها](#بهترین-روش‌ها)

---

## معرفی

سیستم مدیریت SLA (Service Level Agreement) یک سیستم پیشرفته و حرفه‌ای برای تعریف، نظارت و مدیریت قوانین SLA در سیستم تیکتینگ است. این سیستم به شما امکان می‌دهد:

- ✅ تعریف قوانین SLA بر اساس اولویت، دسته‌بندی و دپارتمان
- ✅ نظارت خودکار بر رعایت SLA
- ✅ ارسال هشدار قبل از نقض SLA
- ✅ ارسال اعلان در صورت نقض SLA
- ✅ Escalation خودکار تیکت‌ها
- ✅ گزارش‌گیری و تحلیل عملکرد SLA

---

## ویژگی‌ها

### ✨ ویژگی‌های کلیدی:
- ✅ **تعریف قوانین SLA**: بر اساس اولویت، دسته‌بندی، دپارتمان
- ✅ **نظارت خودکار**: بررسی مداوم وضعیت تیکت‌ها
- ✅ **هشدارهای پیش‌گیرانه**: ارسال هشدار قبل از نقض SLA
- ✅ **اعلان‌های نقض**: اطلاع‌رسانی فوری در صورت نقض
- ✅ **Escalation خودکار**: ارجاع خودکار تیکت‌های در معرض خطر
- ✅ **گزارش‌گیری پیشرفته**: آمار و نمودارهای جامع
- ✅ **لاگ کامل**: ثبت تمام رویدادهای SLA
- ✅ **یکپارچه‌سازی**: با سیستم اعلان‌ها (تلگرام و ایمیل)

---

## نصب و راه‌اندازی

### 1. بررسی Backend

Backend به صورت کامل پیاده‌سازی شده است:
- ✅ مدل‌های دیتابیس (`SLARule`, `SLALog`)
- ✅ Service Layer (`sla_service.py`, `sla_alert_service.py`)
- ✅ API Endpoints (`/api/sla/*`)
- ✅ Background Scheduler برای نظارت خودکار
- ✅ یکپارچه‌سازی با Notification Service

### 2. Frontend

Frontend به صورت کامل پیاده‌سازی شده است:
- ✅ صفحه مدیریت SLA (`SLAManagement.tsx`)
- ✅ نمایش قوانین SLA
- ✅ نمایش لاگ‌های SLA
- ✅ نمودارهای آماری

### 3. راه‌اندازی Scheduler

Scheduler به صورت خودکار در `main.py` راه‌اندازی می‌شود:

```python
# در app/main.py
from app.tasks.sla_tasks import start_sla_scheduler
start_sla_scheduler()
```

**نکته:** Scheduler هر 5 دقیقه یکبار تیکت‌ها را بررسی می‌کند.

---

## استفاده از Frontend

### صفحه مدیریت SLA

#### دسترسی:
1. وارد پنل مدیریت شوید
2. به منوی "⏱️ SLA" بروید
3. یا مستقیماً به `/sla` بروید

---

## مدیریت قوانین SLA

### ایجاد قانون جدید:

1. **کلیک روی "➕ ایجاد قانون SLA جدید"**

2. **پر کردن فرم:**
   - **نام قانون**: نام قانون SLA (مثال: "SLA تیکت‌های بحرانی IT")
   - **توضیحات**: توضیحات اختیاری
   - **اولویت**: انتخاب اولویت (یا "همه اولویت‌ها")
   - **دسته‌بندی**: انتخاب دسته‌بندی (یا "همه دسته‌بندی‌ها")
   - **دپارتمان**: انتخاب دپارتمان (یا "همه دپارتمان‌ها")

3. **زمان‌های هدف:**
   - **زمان پاسخ هدف**: زمان هدف برای پاسخ به تیکت (دقیقه)
   - **زمان حل هدف**: زمان هدف برای حل تیکت (دقیقه)

4. **هشدارها:**
   - **هشدار قبل از مهلت پاسخ**: چند دقیقه قبل از مهلت پاسخ هشدار ارسال شود
   - **هشدار قبل از مهلت حل**: چند دقیقه قبل از مهلت حل هشدار ارسال شود

5. **Escalation:**
   - ☑️ **فعال کردن Escalation**: آیا Escalation فعال باشد؟
   - **Escalation بعد از**: چند دقیقه بعد از ایجاد تیکت Escalation انجام شود

6. **وضعیت:**
   - ☑️ **فعال**: آیا قانون فعال باشد؟

7. **ذخیره قانون**

### ویرایش قانون:

1. کلیک روی "✏️ ویرایش" در ردیف قانون
2. تغییر تنظیمات مورد نظر
3. کلیک روی "💾 ذخیره"

### حذف قانون:

1. کلیک روی "🗑️ حذف" در ردیف قانون
2. تایید حذف

**⚠️ هشدار:** حذف قانون، تمام لاگ‌های مرتبط را نیز حذف می‌کند.

### فعال/غیرفعال کردن قانون:

- کلیک روی دکمه "⏸️" یا "▶️" در ردیف قانون

### فیلترها:

- **وضعیت**: فیلتر بر اساس فعال/غیرفعال

---

## نمایش لاگ‌های SLA

### دسترسی به لاگ‌ها:

1. کلیک روی دکمه "📋 نمایش لاگ‌های SLA"
2. لاگ‌ها در یک جدول نمایش داده می‌شوند

### فیلترهای لاگ:

- **وضعیت پاسخ**: فیلتر بر اساس on_time, warning, breached
- **وضعیت حل**: فیلتر بر اساس on_time, warning, breached
- **Escalation**: فیلتر بر اساس Escalated/Not Escalated

> نکته: مقادیر هر سه فیلتر فقط از طریق منوی کشویی انتخاب می‌شوند و کاملاً با نام وضعیت‌های سیستم (`"" | "on_time" | "warning" | "breached"` برای پاسخ/حل و `"" | "true" | "false"` برای Escalation) همگام هستند؛ بنابراین هیچ مقدار نامعتبری ذخیره یا ارسال نمی‌شود.

### اطلاعات نمایش داده شده:

- **شماره تیکت**: لینک به جزئیات تیکت
- **قانون SLA**: نام قانون اعمال شده
- **وضعیت پاسخ**: on_time ✅, warning ⚠️, breached 🔴
- **وضعیت حل**: on_time ✅, warning ⚠️, breached 🔴
- **مهلت پاسخ**: زمان هدف و زمان واقعی (اگر پاسخ داده شده باشد)
- **مهلت حل**: زمان هدف و زمان واقعی (اگر حل شده باشد)
- **Escalated**: وضعیت Escalation و زمان آن
- **عملیات**: دکمه مشاهده تیکت

### Pagination:

- استفاده از دکمه‌های "قبلی" و "بعدی" برای جابجایی بین صفحات

---

## آمار و نمودارها

### کارت‌های آماری:

صفحه مدیریت SLA شامل 8 کارت آماری است:

1. **کل لاگ‌های SLA**: تعداد کل تیکت‌هایی که SLA دارند
2. **پاسخ در مهلت**: تعداد تیکت‌هایی که در مهلت پاسخ داده شده‌اند
3. **هشدار پاسخ**: تعداد تیکت‌هایی که در حالت هشدار پاسخ هستند
4. **نقض پاسخ**: تعداد تیکت‌هایی که مهلت پاسخ آن‌ها نقض شده است
5. **حل در مهلت**: تعداد تیکت‌هایی که در مهلت حل شده‌اند
6. **هشدار حل**: تعداد تیکت‌هایی که در حالت هشدار حل هستند
7. **نقض حل**: تعداد تیکت‌هایی که مهلت حل آن‌ها نقض شده است
8. **Escalated**: تعداد تیکت‌هایی که Escalated شده‌اند

### نمودارها:

#### 1. نمودار وضعیت پاسخ (Pie Chart)
- نمایش توزیع وضعیت‌های پاسخ (در مهلت، هشدار، نقض شده)
- با رنگ‌بندی: سبز (در مهلت)، زرد (هشدار)، قرمز (نقض شده)

#### 2. نمودار وضعیت حل (Pie Chart)
- نمایش توزیع وضعیت‌های حل (در مهلت، هشدار، نقض شده)
- با رنگ‌بندی: سبز (در مهلت)، زرد (هشدار)، قرمز (نقض شده)

#### 3. نمودار مقایسه‌ای پاسخ و حل (Bar Chart)
- مقایسه وضعیت پاسخ و حل در یک نمودار
- نمایش سه دسته: در مهلت، هشدار، نقض شده

---

## API Reference

### مدیریت قوانین SLA (Admin Only)

#### GET `/api/sla`
لیست قوانین SLA

**Query Parameters:**
- `page`: شماره صفحه (پیش‌فرض: 1)
- `page_size`: تعداد آیتم در هر صفحه (پیش‌فرض: 50)
- `is_active`: فیلتر بر اساس وضعیت فعال

**Response:**
```json
[
  {
    "id": 1,
    "name": "SLA تیکت‌های بحرانی",
    "priority": "critical",
    "response_time_minutes": 60,
    "resolution_time_minutes": 240,
    "is_active": true,
    ...
  }
]
```

#### GET `/api/sla/{sla_id}`
جزئیات یک قانون SLA

#### POST `/api/sla`
ایجاد قانون جدید

**Request Body:**
```json
{
  "name": "SLA تیکت‌های بحرانی",
  "priority": "critical",
  "category": "internet",
  "response_time_minutes": 60,
  "resolution_time_minutes": 240,
  "response_warning_minutes": 30,
  "resolution_warning_minutes": 60,
  "escalation_enabled": true,
  "escalation_after_minutes": 120,
  "is_active": true
}
```

#### PUT `/api/sla/{sla_id}`
ویرایش قانون

#### DELETE `/api/sla/{sla_id}`
حذف قانون

### مدیریت لاگ‌های SLA

#### GET `/api/sla/logs`
لیست لاگ‌های SLA (Admin Only)

**Query Parameters:**
- `page`: شماره صفحه
- `page_size`: تعداد آیتم در هر صفحه
- `ticket_id`: فیلتر بر اساس تیکت
- `sla_rule_id`: فیلتر بر اساس قانون SLA
- `response_status`: فیلتر بر اساس وضعیت پاسخ (on_time, warning, breached)
- `resolution_status`: فیلتر بر اساس وضعیت حل (on_time, warning, breached)
- `escalated`: فیلتر بر اساس Escalation (true/false)

**Response:**
```json
[
  {
    "id": 1,
    "ticket_id": 123,
    "sla_rule_id": 1,
    "ticket_number": "TKT-2024-001",
    "sla_rule_name": "SLA تیکت‌های بحرانی",
    "target_response_time": "2024-01-23T10:00:00Z",
    "target_resolution_time": "2024-01-23T14:00:00Z",
    "actual_response_time": "2024-01-23T09:45:00Z",
    "response_status": "on_time",
    "resolution_status": "warning",
    "escalated": false,
    ...
  }
]
```

#### GET `/api/sla/ticket/{ticket_id}`
دریافت لاگ SLA برای یک تیکت

**Response:**
```json
{
  "id": 1,
  "ticket_id": 123,
  "sla_rule_id": 1,
  "target_response_time": "2024-01-23T10:00:00Z",
  "target_resolution_time": "2024-01-23T14:00:00Z",
  "response_status": "on_time",
  "resolution_status": "warning",
  ...
}
```

### گزارش‌های SLA

#### GET `/api/reports/sla-compliance`
گزارش رعایت SLA

**Response:**
```json
{
  "total_tickets_with_sla": 150,
  "response_on_time": 120,
  "response_warning": 20,
  "response_breached": 10,
  "resolution_on_time": 100,
  "resolution_warning": 30,
  "resolution_breached": 20,
  "escalated_count": 5,
  "response_compliance_rate": 92.31,
  "resolution_compliance_rate": 83.33
}
```

#### GET `/api/reports/sla-by-priority`
گزارش SLA بر اساس اولویت

**Response:**
```json
[
  {
    "priority": "critical",
    "total_tickets": 50,
    "response_on_time": 45,
    "response_breached": 5,
    "resolution_on_time": 40,
    "resolution_breached": 10,
    "response_compliance_rate": 90.0,
    "resolution_compliance_rate": 80.0
  }
]
```

---

## بهترین روش‌ها

### 1. تعریف قوانین SLA

✅ **خوب:**
- تعریف قوانین خاص برای اولویت‌های بحرانی
- تعریف قوانین جداگانه برای هر دسته‌بندی
- استفاده از Escalation برای تیکت‌های مهم

❌ **بد:**
- تعریف یک قانون کلی برای همه تیکت‌ها
- عدم استفاده از هشدارها
- عدم فعال‌سازی Escalation

### 2. تنظیم زمان‌ها

- **زمان پاسخ**: معمولاً 30-120 دقیقه برای اولویت‌های بالا
- **زمان حل**: معمولاً 4-8 ساعت برای اولویت‌های بالا
- **هشدار پاسخ**: 20-30% از زمان پاسخ
- **هشدار حل**: 20-30% از زمان حل

### 3. استفاده از Escalation

- Escalation را برای تیکت‌های بحرانی فعال کنید
- زمان Escalation را 50-70% از زمان حل تنظیم کنید
- Escalation را فقط برای اولویت‌های بالا استفاده کنید

### 4. نظارت و بررسی

- به صورت منظم لاگ‌های SLA را بررسی کنید
- تیکت‌های نقض شده را تحلیل کنید
- قوانین SLA را بر اساس عملکرد واقعی تنظیم کنید

### 5. گزارش‌گیری

- از نمودارها برای تحلیل روند استفاده کنید
- Compliance Rate را به صورت منظم بررسی کنید
- گزارش‌های SLA را به مدیریت ارائه دهید

---

## مثال‌های کاربردی

### مثال 1: قانون SLA برای تیکت‌های بحرانی اینترنت

```json
{
  "name": "SLA تیکت‌های بحرانی اینترنت",
  "priority": "critical",
  "category": "internet",
  "response_time_minutes": 30,
  "resolution_time_minutes": 120,
  "response_warning_minutes": 10,
  "resolution_warning_minutes": 30,
  "escalation_enabled": true,
  "escalation_after_minutes": 90,
  "is_active": true
}
```

**توضیح:**
- پاسخ در 30 دقیقه
- حل در 120 دقیقه (2 ساعت)
- هشدار 10 دقیقه قبل از مهلت پاسخ
- هشدار 30 دقیقه قبل از مهلت حل
- Escalation بعد از 90 دقیقه

### مثال 2: قانون SLA عمومی برای همه تیکت‌ها

```json
{
  "name": "SLA عمومی",
  "priority": null,
  "category": null,
  "department_id": null,
  "response_time_minutes": 120,
  "resolution_time_minutes": 480,
  "response_warning_minutes": 30,
  "resolution_warning_minutes": 60,
  "escalation_enabled": false,
  "is_active": true
}
```

**توضیح:**
- پاسخ در 120 دقیقه (2 ساعت)
- حل در 480 دقیقه (8 ساعت)
- فقط هشدار (بدون Escalation)

---

## عیب‌یابی

### مشکل: SLA برای تیکت ایجاد نمی‌شود

**راه‌حل:**
1. بررسی کنید قانون SLA فعال باشد (`is_active = true`)
2. بررسی کنید قانون با اولویت، دسته‌بندی و دپارتمان تیکت مطابقت داشته باشد
3. بررسی کنید لاگ‌های سیستم برای خطاها

### مشکل: هشدارها ارسال نمی‌شوند

**راه‌حل:**
1. بررسی کنید Scheduler در حال اجرا باشد
2. بررسی کنید تنظیمات Notification Service
3. بررسی کنید کاربران Telegram Chat ID یا Email داشته باشند

### مشکل: Escalation انجام نمی‌شود

**راه‌حل:**
1. بررسی کنید `escalation_enabled = true` باشد
2. بررسی کنید `escalation_after_minutes` تنظیم شده باشد
3. بررسی کنید زمان Escalation گذشته باشد

---

## پشتیبانی

برای سوالات و مشکلات:
- 📧 ایمیل: support@iranmehr.com
- 📚 مستندات: [docs/SLA_MANAGEMENT.md](SLA_MANAGEMENT.md)
- 🐛 Issues: [GitHub Issues](https://github.com/your-repo/issues)

---

**آخرین به‌روزرسانی:** 2025-01-23  
**نسخه:** 1.0.0


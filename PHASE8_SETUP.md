# راهنمای فاز ۸: پنل وب مدیریتی / Phase 8: Web Admin Panel

## 🎯 هدف
ساخت پنل وب برای مدیریت سیستم تیکتینگ (React + TypeScript + Vite) با صفحات پایه: ورود، داشبورد، لیست تیکت‌ها، جزئیات تیکت.

## ✅ انتخاب فناوری
- React 18 + TypeScript
- Vite برای توسعه سریع
- Fetch/axios برای ارتباط با API (در حال حاضر از fetch ساده استفاده می‌کنیم)
- ساده‌سازی اولیه UI (بدون CSS framework – قابل افزودن Tailwind در ادامه)

## 🚀 راه‌اندازی
1) نصب وابستگی‌ها:
```bash
cd web_admin
npm install
npm run dev
```
سپس در مرورگر به آدرس نشان‌داده‌شده (معمولاً http://localhost:5173) بروید.

2) تنظیم آدرس API:
- فایل `web_admin/src/services/api.ts` مقدار `API_BASE_URL` را از `.env` یا مقدار پیش‌فرض `http://localhost:8000` می‌خواند. در صورت نیاز ویرایش کنید.

## 📁 ساختار پوشه
```
web_admin/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── src/
    ├── App.tsx
    ├── main.tsx
    ├── pages/
    │   ├── Dashboard.tsx
    │   ├── Login.tsx
    │   ├── Tickets.tsx
    │   ├── TicketDetail.tsx
    │   ├── Branches.tsx
    │   └── Users.tsx
    └── services/
        └── api.ts
```

## 🧩 صفحات
- Login: دریافت توکن و ذخیره در localStorage + دریافت پروفایل کاربر
- Dashboard: خلاصه آمار
- Tickets: لیست تیکت‌ها با صفحه‌بندی، فیلتر و جستجو
- Ticket Detail: مشاهده و تغییر وضعیت (ادمین)
- Branches: مدیریت شعب و ویرایش مشخصات
- Users: مدیریت کامل کاربران (ایجاد، ویرایش، حذف، نقش‌ها، انتساب شعبه، فعال/غیرفعال)

## 🔐 احراز هویت
- پس از ورود، توکن در localStorage ذخیره و پروفایل کاربر هم ذخیره می‌شود.
- نقش کاربر برای نمایش لینک‌های ناوبری و سطوح دسترسی بررسی می‌شود (Users فقط برای admin/central_admin).

## 🧪 تست
1) API را اجرا کنید:
```bash
uvicorn app.main:app --reload
```
2) وب‌اپ را اجرا کنید:
```bash
cd web_admin
npm run dev
```
3) با کاربر `admin/admin123` وارد شوید و لیست تیکت‌ها را ببینید.

## 📌 گام‌های بعدی
- استایل‌دهی پیشرفته‌تر (Tailwind)
- داشبورد کاملاً پویا
- مدیریت کاربران با سطوح دسترسی بیشتر (در حال حاضر انجام شد)
- اتصال به سیستم اعلان

---

**تاریخ:** 2025-11-12


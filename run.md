# راهنمای اجرا (Windows 11) - سیستم تیکتینگ ایرانمهر

این سند به صورت کامل و مرحله‌به‌مرحله توضیح می‌دهد چطور پروژه را روی ویندوز 11 اجرا کنید.

---

## 📋 فهرست مطالب

1. [پیش‌نیازها](#۰-پیشنیازها)
2. [نصب و راه‌اندازی](#۱-نصب-و-راهاندازی)
3. [سرویس‌های مورد نیاز](#۲-سرویسهای-مورد-نیاز)
4. [اجرای سرویس‌ها](#۳-اجرای-سرویسها)
5. [تست و استفاده](#۴-تست-و-استفاده)
6. [عیب‌یابی](#۵-عیبیابی)

---

## ۰) پیش‌نیازها

### نرم‌افزارهای مورد نیاز:
- **Python 3.10+** (پیشنهادی 3.10 یا 3.11)
  - بررسی: `python --version`
  - دانلود: `https://www.python.org/downloads/`
  - ⚠️ حتماً گزینه "Add Python to PATH" را تیک بزنید
- **Node.js 18+** و npm
  - بررسی: `node -v` و `npm -v`
  - دانلود: نسخه LTS از `https://nodejs.org/`
- **Git** (اختیاری اما پیشنهاد می‌شود)

---

## ۱) نصب و راه‌اندازی

### مرحله ۱: دریافت پروژه
```powershell
cd C:\PersonalProject
git clone <repository-url> imehrTicketing
cd .\imehrTicketing
```

### مرحله ۲: ساخت و فعال‌سازی محیط مجازی (Backend)
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

⚠️ **اگر PowerShell اجرای اسکریپت را مسدود کرد:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
سپس دوباره `.\venv\Scripts\Activate.ps1` را اجرا کنید.

### مرحله ۳: نصب وابستگی‌های پایتون
```powershell
pip install -r requirements.txt
```

**پکیج‌های کلیدی:**
- FastAPI, Uvicorn (سرور وب)
- SQLAlchemy (دیتابیس)
- python-telegram-bot (ربات تلگرام)
- openpyxl (خروجی Excel)

### مرحله ۴: تنظیم فایل محیطی (.env)
```powershell
copy .\env.example .\.env
```

**فایل `.env` را باز کنید و مقادیر زیر را تنظیم کنید:**

```env
# کلید امنیتی (با اسکریپت زیر می‌توانید بسازید)
SECRET_KEY=your-secret-key-here

# تنظیمات دیتابیس
DATABASE_URL=sqlite:///./ticketing.db

# تنظیمات API
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# تنظیمات ربات تلگرام (اختیاری)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# آدرس API برای ربات
API_BASE_URL=http://127.0.0.1:8000

# CORS (برای پنل وب)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8000,http://localhost:5173,http://127.0.0.1:5173
```

**ساخت SECRET_KEY:**
```powershell
python .\scripts\generate_secret_key.py
```

### مرحله ۵: مقداردهی اولیه دیتابیس
```powershell
python .\scripts\init_db.py
python .\scripts\create_admin.py
```

**اطلاعات پیش‌فرض ادمین:**
- Username: `admin`
- Password: `admin123`

⚠️ **توجه:** در Production حتماً رمز عبور را تغییر دهید!

---

## ۲) سرویس‌های مورد نیاز

برای اجرای کامل سیستم، **۳ سرویس** باید همزمان اجرا شوند:

### ✅ سرویس ۱: FastAPI Backend (اجباری)
- **نقش:** API اصلی سیستم
- **پورت:** 8000
- **دستور:** `uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload`
- **وضعیت:** باید همیشه اجرا باشد

### ✅ سرویس ۲: Telegram Bot (اختیاری)
- **نقش:** ربات تلگرام برای کاربران
- **پورت:** ندارد (از API استفاده می‌کند)
- **دستور:** `python -m app.telegram_bot.run`
- **وضعیت:** فقط اگر می‌خواهید ربات کار کند
- **پیش‌نیاز:** FastAPI باید اجرا باشد

### ✅ سرویس ۳: React Frontend (اختیاری)
- **نقش:** پنل مدیریتی وب
- **پورت:** 5173 (Vite default)
- **دستور:** `npm run dev` (در پوشه `web_admin`)
- **وضعیت:** فقط برای استفاده از پنل وب

---

## ۳) اجرای سرویس‌ها

### 🚀 روش ۱: اجرای کامل (توصیه می‌شود)

**ترمینال ۱: FastAPI Backend**
```powershell
cd C:\PersonalProject\imehrTicketing
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```
✅ باید پیام `Application startup complete` را ببینید
✅ مستندات API: `http://127.0.0.1:8000/docs`

**ترمینال ۲: Telegram Bot** (اختیاری)
```powershell
cd C:\PersonalProject\imehrTicketing
.\venv\Scripts\Activate.ps1
python -m app.telegram_bot.run
```
✅ باید پیام `Telegram Bot started and polling for updates` را ببینید

**ترمینال ۳: React Frontend** (اختیاری)
```powershell
cd C:\PersonalProject\imehrTicketing\web_admin
npm install  # فقط بار اول
npm run dev
```
✅ باید آدرس `http://localhost:5173` نمایش داده شود

---

### 📝 توضیحات دستورات ربات تلگرام

**❌ دستور اشتباه:**
```powershell
python -m app.telegram_bot.bot  # این کار نمی‌کند!
```

**✅ دستور درست:**
```powershell
python -m app.telegram_bot.run  # این دستور صحیح است
```

**چرا؟**
- `app.telegram_bot.run` یک ماژول runner است که `start_bot()` را به درستی فراخوانی می‌کند
- `app.telegram_bot.bot` فقط فایل bot است و مستقیماً قابل اجرا نیست

**نکته:** اگر `TELEGRAM_BOT_TOKEN` در `.env` خالی باشد، ربات اجرا نمی‌شود.

---

### 🔄 روش ۲: اجرای ربات همراه با FastAPI

اگر `TELEGRAM_BOT_TOKEN` در `.env` تنظیم شده باشد، ربات به صورت خودکار همراه با FastAPI اجرا می‌شود:

```powershell
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

⚠️ **توجه:** در این حالت، اگر FastAPI متوقف شود، ربات هم متوقف می‌شود.

---

## ۴) تست و استفاده

### تست سریع:

1. **تست API:**
   - باز کردن: `http://127.0.0.1:8000/docs`
   - تست endpoint `/api/auth/login` با:
     - username: `admin`
     - password: `admin123`

2. **تست ربات تلگرام:**
   - در تلگرام ربات را پیدا کنید
   - دستور `/start` را بزنید
   - دستور `/login` را بزنید و با `admin/admin123` وارد شوید

3. **تست پنل وب:**
   - باز کردن: `http://localhost:5173`
   - لاگین با `admin/admin123`
   - بررسی داشبورد و لیست تیکت‌ها

---

## ۵) عیب‌یابی

### مشکل: "ModuleNotFoundError"
**راه‌حل:**
```powershell
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### مشکل: خطای CORS در مرورگر
**راه‌حل:**
- مطمئن شوید `CORS_ORIGINS` در `.env` شامل `http://localhost:5173` است
- FastAPI را ری‌استارت کنید

### مشکل: ربات به API متصل نمی‌شود
**راه‌حل:**
- مطمئن شوید FastAPI روی پورت 8000 اجرا است
- `API_BASE_URL` در `.env` را بررسی کنید: `http://127.0.0.1:8000`
- ربات را ری‌استارت کنید

### مشکل: پورت 8000 مشغول است
**راه‌حل:**
```powershell
# پورت را عوض کنید
uvicorn app.main:app --host 127.0.0.1 --port 8080 --reload
```
⚠️ سپس `API_BASE_URL` و `CORS_ORIGINS` را هم به‌روزرسانی کنید.

### مشکل: ربات اجرا نمی‌شود
**راه‌حل:**
- `TELEGRAM_BOT_TOKEN` در `.env` را بررسی کنید
- مطمئن شوید FastAPI اجرا است
- از دستور صحیح استفاده کنید: `python -m app.telegram_bot.run`

---

## 📊 خلاصه دستورات

### برای شروع کامل سیستم:

**ترمینال ۱ (Backend):**
```powershell
cd C:\PersonalProject\imehrTicketing
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

**ترمینال ۲ (Bot - اختیاری):**
```powershell
cd C:\PersonalProject\imehrTicketing
.\venv\Scripts\Activate.ps1
python -m app.telegram_bot.run
```

**ترمینال ۳ (Frontend - اختیاری):**
```powershell
cd C:\PersonalProject\imehrTicketing\web_admin
npm run dev
```

---

## 🎯 چک‌لیست اجرا

- [ ] Python 3.10+ نصب است
- [ ] Node.js 18+ نصب است
- [ ] محیط مجازی ساخته و فعال شده
- [ ] وابستگی‌های Python نصب شده (`pip install -r requirements.txt`)
- [ ] فایل `.env` تنظیم شده
- [ ] دیتابیس مقداردهی شده (`python .\scripts\init_db.py`)
- [ ] ادمین ساخته شده (`python .\scripts\create_admin.py`)
- [ ] FastAPI اجرا است (پورت 8000)
- [ ] (اختیاری) ربات تلگرام اجرا است
- [ ] (اختیاری) پنل وب اجرا است (پورت 5173)

---

## 📚 منابع بیشتر

- **مستندات API:** `http://127.0.0.1:8000/docs`
- **پنل وب:** `http://localhost:5173`
- **لاگ‌ها:** `logs/app.log`

---

**آخرین به‌روزرسانی:** 2025-01-12

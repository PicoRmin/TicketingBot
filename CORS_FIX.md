# راهنمای رفع مشکل CORS

## 🔴 مشکل

خطای CORS در Console مرورگر:
```
Access to fetch at 'http://127.0.0.1:8000/api/tickets?page=1&page_size=10' 
from origin 'http://localhost:5173' has been blocked by CORS policy: 
No 'Access-Control-Allow-Origin' header is present on the requested resource.
```

## ✅ راه‌حل

### مرحله ۱: بررسی تنظیمات CORS

در فایل `.env` مطمئن شوید که این خط وجود دارد:

```env
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8000,http://localhost:5173,http://127.0.0.1:5173,http://127.0.0.1:8000
```

### مرحله ۲: ری‌استارت Backend

**مهم**: بعد از تغییر `.env`، حتماً Backend را ری‌استارت کنید:

```powershell
# Ctrl+C برای توقف
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

### مرحله ۳: بررسی لاگ‌ها

بعد از ری‌استارت، در لاگ‌ها باید ببینید:
```
INFO:     Application startup complete.
```

### مرحله ۴: تست اتصال

در Console مرورگر (F12) این دستور را بزنید:

```javascript
fetch('http://127.0.0.1:8000/health')
  .then(r => r.json())
  .then(console.log)
```

اگر خطای CORS نداد، مشکل حل شده است! ✅

## 🔍 عیب‌یابی

### اگر هنوز خطا دارید:

1. **بررسی Backend در حال اجرا است:**
   ```powershell
   # در مرورگر یا PowerShell:
   Invoke-WebRequest -Uri "http://127.0.0.1:8000/health"
   ```

2. **بررسی CORS در `.env`:**
   - مطمئن شوید `http://localhost:5173` در لیست است
   - بدون فاصله اضافی
   - کاما بین آدرس‌ها

3. **بررسی Backend ری‌استارت شده:**
   - بعد از تغییر `.env` حتماً Backend را ری‌استارت کنید
   - تغییرات `.env` فقط با ری‌استارت اعمال می‌شوند

4. **بررسی Console مرورگر:**
   - F12 → Console
   - خطاهای دقیق را ببینید

---

**نکته**: اگر از `http://127.0.0.1:5173` استفاده می‌کنید، باید `http://127.0.0.1:5173` هم در CORS_ORIGINS باشد.

